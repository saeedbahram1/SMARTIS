Flutter generates the real Windows runner files automatically.

After extracting this project:
1. cd frontend
2. flutter create .
3. flutter pub get
4. flutter run -d windows

The Dart source files in lib/ are already supplied.
The generated Windows runner will be compatible with your installed Flutter SDK.
