Whisper model directory.

Place your existing model here:

backend/models/whisper-small/

The model must be compatible with faster-whisper/CTranslate2.
If your existing `WHISPER_MODEL=models/whisper-small` uses another
Whisper format, do not convert it yet; tell us its folder contents
in Stage 2 and we will adapt the loader.
